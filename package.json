{
  "name": "el-b2c-workspace-cleaner-action",
  "version": "0.0.0",
  "description": "Clean up workspace for self-hosted runner",
  "scripts": {
    "test": "npx mocha --require esm ./test/**",
    "lint": "npx eslint ./src"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rockychen-ef/el-b2c-workspace-cleaner-action.git"
  },
  "dependencies": {
    "esm": "^3.2.25"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "eslint": "^6.4.0",
    "mocha": "^6.2.2",
    "npx": "^10.2.0",
    "shelljs": "^0.8.3"
  }
}
